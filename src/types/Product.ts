// 🎯 INTERFACE SÚPER SIMPLE - Solo lo que la UI necesita
export interface Product {
    id: string;
    name: string;
    price: number;
    image: string;
    rating: number;
    discount: number;
}